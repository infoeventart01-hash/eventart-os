"use client";
import { FormEvent, useState } from "react";
export default function ResetPassword(){const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const form=new FormData(event.currentTarget);if(form.get("password")!==form.get("confirm")){setMessage("The passwords do not match.");setBusy(false);return}const response=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password:form.get("password")})});const data=await response.json();if(response.ok){setMessage("Password updated. Redirecting to EventArt…");setTimeout(()=>window.location.assign("/"),900)}else{setMessage(data.error||"Unable to update password.");setBusy(false)}}return <main className="auth-page"><section className="auth-card"><img src="/eventart-logo-transparent.png" alt="EventArt"/><p className="eyebrow">SECURE ACCOUNT</p><h1>Choose a new password</h1>{message&&<div className="auth-message">{message}</div>}<form onSubmit={submit}><label>New password<input name="password" type="password" autoComplete="new-password" minLength={10} required/></label><label>Confirm password<input name="confirm" type="password" autoComplete="new-password" minLength={10} required/></label><button className="gold-button" disabled={busy}>{busy?"Saving…":"Save Password"}</button></form></section></main>}
