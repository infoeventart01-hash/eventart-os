"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
export default function ForgotPassword(){const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const form=new FormData(event.currentTarget);try{const response=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:form.get("email")})});const data=await response.json();setMessage(data.message||data.error||"Check your email for a reset link.")}finally{setBusy(false)}}return <main className="auth-page"><section className="auth-card"><img src="/eventart-logo-transparent.png" alt="EventArt"/><p className="eyebrow">ACCOUNT RECOVERY</p><h1>Reset your password</h1><p>Enter the email used for your EventArt account.</p>{message&&<div className="auth-message">{message}</div>}<form onSubmit={submit}><label>Email<input name="email" type="email" autoComplete="email" required/></label><button className="gold-button" disabled={busy}>{busy?"Sending…":"Send Reset Link"}</button></form><Link href="/login">Back to Sign In</Link></section></main>}
